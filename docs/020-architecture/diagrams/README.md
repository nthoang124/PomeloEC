# Sơ đồ Kiến trúc (Architecture Diagrams)

Thư mục này được sử dụng để chứa các export hình ảnh (PNG, SVG) độ phân giải cao hoặc các file nguồn `.drawio`, `.excalidraw` nếu có liên quan đến kiến trúc hệ thống của dự án. Với kiến trúc C4 Model căn bản, mã nguồn Mermaid đã được nhúng trực tiếp vào file `system-overview.md`.
